export default function ContentThree() {
    return (<p className="text-base text-gray-700 line-clamp-3 max-w-xs">We have three components here. This is part of our learning journey 
      where we practice building modular UI pieces. By organizing our 
      code into components, we make it reusable, readable, and much 
      easier to maintain as the project grows larger over time.</p>);
}